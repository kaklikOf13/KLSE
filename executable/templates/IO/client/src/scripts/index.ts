
import { Game } from "common/scripts/game.ts"
const g=new Game()
console.log(g)
/*import { createCanvas,Renderer } from "KLSE/CLIENT"
const canvas=createCanvas()
const renderer=new Renderer(canvas)*/
